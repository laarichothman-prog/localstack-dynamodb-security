services:
  localstack:
    image: localstack/localstack:3.8.1
    container_name: localstack-main
    ports:
      - "4566:4566"
    environment:
      - SERVICES=dynamodb
      - DEBUG=1
      - AWS_DEFAULT_REGION=us-east-1